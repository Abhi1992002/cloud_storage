import { NewPasswordForm } from "@/components/auth/new-password-form";
import React from "react";

type NewPasswordPageProps = {};

const NewPasswordPage = ({}: NewPasswordPageProps) => {
  return (
    <div>
      <NewPasswordForm />
    </div>
  );
};

export default NewPasswordPage;
